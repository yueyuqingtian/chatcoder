# 更新日志

> 本文件随应用打包（electron extraResources），作为「更新历史」在离线场景的回落数据源。
> 在线时设置 → 关于页优先展示 GitHub Releases 的完整历史。

## v0.5.12

- 新增（内置 MCP：数据库连接）：应用内置一个默认**关闭**的 MCP 连接器「数据库连接」，可在「设置 → 拓展 → 连接器」中启用；支持按**项目**添加多个连接（MySQL / MariaDB、PostgreSQL、SQL Server），主机/端口/库名/账号/密码可配并支持一键测试连接
- 新增（数据库权限管控）：每个项目独立的权限策略——读 / 写 / DDL 三个开关 + 是否强制审批 + 单次返回行数上限 + 执行超时。**服务端强制门控**（不是前端开关说了算）：`db_query` 只放行只读语句、`db_execute` 只放行 DML、`db_ddl` 单独受 DDL 权限管辖；多语句拼接（如 `SELECT 1; DROP TABLE t`）一律拒绝，防"只读开头绕过权限"
- 新增（语句与结果可见）：AI 执行的每条 SQL 与结果都在消息流中结构化呈现——语句等宽高亮可复制、执行摘要（连接/耗时/影响行数/结果行数）、结果表格（前 30 行），被权限拒绝时原因原样展示
- 新增（内置 MCP：开发调试）：应用内置第二个默认**关闭**的连接器「开发调试」——① 接口调用与批量探测（`debug_http` / `debug_probe`）；② Web 前端断点调试（基于 CDP：下断点、等待命中、步入/跳过/跳出、继续、帧上求值、读取局部变量）；③ Java 断点调试（基于 JDWP：附加、按类+行下断点、等待命中、读取线程栈帧）；④ 状态查询
- 新增（调试对用户可见）：命中断点时后端广播 `debug.paused` 事件，前端调试面板与消息流实时显示「已暂停：文件:行号（函数名）」+ 调用栈 + 变量表，并提供 继续 / 跳过 / 步入 / 跳出 / 停止调试 按钮；调试面板同时给出 JDWP 启动参数一键复制与 Web 调试端口配置
- 新增（/ 菜单分区）：`/` 菜单按「快捷命令 / 技能 / 连接器 / 插件」四段分区展示；技能名统一显示可读名（`display_name || name`，此前显示 `README`、`skill` 这类文件名标识符，与设置页不一致）；连接器与插件可插入引用 chip，发送时随消息附加「使用连接器：<名称>」「使用插件：<名称>」引导模型
- 优化：内置 MCP 的风险等级改由**服务自身申报**（`annotations.riskLevel`）——比按工具名猜测可靠（`db_query` 含 "query" 会被猜成只读，但它到底是读还是写只有服务知道）；申报值不合法时回退关键词推断
- 修复：内置 MCP 的开发/打包两种启动形态统一——开发态 `python -m app.mcp_servers.<name>`，打包态由 `chatcoder-server.exe --mcp-server <name>` 分流（exe 不支持 `-m`）；相关模块与内置插件目录一并纳入 PyInstaller 收集清单

## v0.5.11

- 新增（拓展中心）：原先分散的「插件 / 技能 / MCP 服务器」三个设置页收拢为一个「拓展」页，按插件 / 技能 / 连接器三个子标签分区展示（原技能仓库、本地导入、扫描导入等功能全部保留）；左侧面板原「技能」入口同步改名「拓展」并复用同一面板，保证两处内容完全一致
- 新增（插件市场）：插件子标签改为市场形态（对齐参考项目）——分类条 + 搜索 + 热门/最新排序 + 卡片网格 + 详情抽屉，支持从本地目录或 Git 仓库安装、启用/停用与卸载；插件清单 `plugin.json` 字段与参考项目同构（name/displayName/version/description/descriptionZh/author/keywords/category/tags/skills/logo/marketplaceName/defaultEnabled），注册表沿用 `installed_plugins.json` 的 `{plugins: {"<name>@<market>": [...]}, version: 2}` 结构
- 新增（插件贡献）：插件 `skills/` 下的技能以 `source="plugin"` 注册进技能表（名称加 `<plugin>:` 前缀避免重名覆盖），随插件启停同步开关；插件内 `mcp.json` 的 MCP 会出现在「连接器」区
- 新增：内置插件目录 `server/app/data/plugin_catalog.json`（12 个分类条目）作为市场初始内容；不接线上市场接口（无可用的公开稳定 API），改为「同构格式 + 内置目录 + 可从目录/Git 安装」
- 修复：`plugin_catalog.json` 未加入 PyInstaller 打包清单——源码运行正常但打包版的市场页会空空如也；同时按项目既有口径用 `sys._MEIPASS` 定位解包资源（`Path(__file__)` 在 PyInstaller 归档下不是真实磁盘路径）
- 修复（/ 菜单）：技能名显示为文件名标识符（如 `README`、`skill`）而非可读名——`/` 菜单与左侧面板此前用 `name`（文件名 stem），而设置页用 `display_name`（来自 frontmatter 或首个 `# 标题`），三处口径不一致；现菜单与左面板统一显示 `display_name || name`，引用值仍用稳定的 `name`
- 新增（/ 菜单）：分区展示「快捷命令 / 技能 / 连接器」，连接器区列出已启用的 MCP（含内置与插件贡献），选中后在输入框上方生成连接器引用 chip，发送时随消息附加「使用连接器：<名称>」引导模型走对应 MCP
- 修复：左侧面板原「技能」页含死链接（跳转到已合并的 `tab: "skills"`）且技能名与设置页不一致；该页已由共用的「拓展」面板替代并删除

## v0.5.10

- 新增（工作树 / worktree）：设置新增「工作树」页，按项目管理同一项目的多个隔离工作区（支持创建、查看 git 状态、合并到主工作区、删除）；左侧项目菜单新增「创建工作树」一键入口，工作树在侧栏作为带分支图标的独立工作区显示（可在其中直接开新会话），其菜单支持「合并到主工作区 / 删除工作树」
- 新增（合并到主工作区）：IDEA 式三栏冲突解决——先给差异文件列表（状态 + 冲突标记），进入单文件后左=工作树、中=合并结果（可直接编辑/粘贴）、右=主工作区，冲突块支持「接受左侧 / 接受右侧 / 两者都保留」与上下跳转，并可请求 **AI 合并建议**（针对当前冲突块）或**一键 AI 智能合并**（对所有文件依次求建议），全部解决后一次提交
- 修复：工作树建在 `<repo>/.chatcoder/worktrees/` 下，导致主仓库恒被判为「有未提交变更」——合并与删除会被本工具自己的目录阻塞；现创建时把 `.chatcoder/` 写入 `.git/info/exclude`（本地忽略，不改用户 `.gitignore`），并在脏状态判定中过滤该目录
- 修复：永久删除会话后仍能在归档页看到——`DELETE /sessions/{id}` 只有归档语义；新增 `permanent=true` 走物理删除并级联清理 messages/turns/tasks/agents/audit/rollback/会话记忆，失败或不存在时返回 404
- 修复：模型注册表缺项——`ToolCall` 与 `RollbackWrite` 未纳入 `models/__init__.py`，导致全新数据库**不会创建** `tool_calls`/`rollback_writes` 表（幂等键与回滚记录在全新部署上静默失效）
- 修复：`AuditLog` 在 `audit.py` 与 `tool_call.py` 中重复定义（同一张 `audit_logs` 表但列集合不同），两者同时导入即触发 SQLAlchemy 「Table already defined」；删除遗留重复定义，统一以 `audit.py` 为准
- 新增：设置 → 归档 支持按名称搜索、多选与批量操作（批量恢复 / 批量**永久删除**，删除带不可恢复的二次确认）

## v0.5.9

- 修复（串会话，重要）：当前会话可能读到**另一个会话**的计划文档——`_find_plan_document` 的兜底分支扫描 `ai/chatcoder-plan*.md` 时**不区分归属**、直接按 mtime 取最新，于是同一工作区里别的会话刚写出的方案会被本会话命中（表现为"需求全集/执行内容串台"）；现按文件名里的归属会话 id 判定（新增 `_plan_doc_belongs_to_other`，只丢弃能**确定**属于别的会话的文档，纯时间戳命名仍按既有契约保留），并补 3 项回归测试
- 修复（串会话）：`message.created` 事件处理缺少会话归属校验——一旦 WS 切换/重连存在竞态（旧 socket 尚在派发、或断线补偿跨越会话边界），别的会话的消息会被追加进当前消息流；现按消息自带的 `session_id` 复核后丢弃
- 修复：点击「确认执行」开始执行后，计划卡又重新显示「取消 / 确认执行」按钮——`task.proposed` 事件处理**无条件**把卡片状态写成 `awaiting_confirmation`，而计划模式确认后是同一 turn 继续执行，执行阶段的事件会把已确认的卡片打回待确认；现规定已进入终态（confirmed/cancelled/superseded）的卡片不得被后续 proposed 改写，也不再重新置起待确认入口
- 修复：`BrowserPanel` 引用了 `frameBridge` 的 `isGuestFrame` 却漏了导入，导致类型检查报错 `Cannot find name 'isGuestFrame'`（此前会阻断构建）
- 修复：面板分隔线「有时看不到」——分隔线用 `--border`（仅 10% 透明度）绘制，而左右面板背景同为 `--bg-panel`，1px 细线在非整数像素处被抗锯齿吃掉；现新增专用令牌 `--divider`（明/暗两档更实的中性色）
- 修复：拖拽面板分隔条调整消息流宽度时，消息位置会随文本重排而漂移——宽度变化导致每条消息高度改变、虚拟列表重新测量，同一个 `scrollTop` 不再对应同一条内容；现引入"视口锚点"：宽度变化当帧记录首条可见项及其视口偏移，重测收敛后按锚点还原（原本贴底则保持贴底，否则"看哪条就停在哪条"）
- 修复：任务已执行完成（如 5/5）后，胶囊进度仍卡在最后一步——上一版为修"停止后重发丢进度"改成"按 turn 倒序找第一个仍有未完成项的轮次"，会一路回溯到更早被中断的陈旧清单；现改为**只认最近一个含步骤的 turn**：有未完成项就展示，全完成即不展示（两个症状同时修好）
- 修复：胶囊聚焦时出现颜色/边框变化——现为聚焦零视觉变化（与 `focus.css` 既有约定一致）
- 修复：胶囊浮层不在胶囊正上方且文字发虚——浮层此前相对"某一段"定位，且居中用的 `transform` 被入场动画终态 `transform: none` 覆盖；现统一在胶囊容器级渲染、用独立的 `translate` 属性居中、动画不再保留填充（`both`），文字按整数像素光栅化
- 修复：胶囊与上方消息流的间隔过大/遮挡文字——胶囊尺寸回调至约 30px，消息流底部恒定预留由 56px 收敛到 44px（仍为恒定预留，避免任务开始/结束时高度跳变）
- 修复：胶囊浮层滚动条过粗——统一收细到 3px 并隐藏两端箭头
- 优化：右侧任务摘要面板与输入框上方胶囊**同源**——抽出 `chat/taskProgress.ts` 作为任务进度行的唯一数据源（旧实现右面板只看 `max(turn_id)` 且无步骤时回退到 `request` 任务，而 `request` 的标题就是用户消息文本，导致两处显示不一致）

## v0.5.8

- 修复：右侧浏览器面板折叠/展开后整页报「应用遇到意外错误：The WebView must be attached to the DOM and the dom-ready event emitted before this method can be called.」——Electron 的 `<webview>` 在未 attach 到 DOM 或 dom-ready 未触发时，`executeJavaScript` / `capturePage` / `openDevTools` 会**同步抛错**（`.catch()` 抓不住），而面板折叠会卸载 `BrowserPanel`、展开时重建 webview，标注探针的注入与 cleanup 恰好都在该窗口内直接调用，异常冒泡到顶层 ErrorBoundary 即整页白屏；现新增 `frameBridge` 统一收口 guest 调用（按标签页跟踪就绪状态，未就绪时挂起、就绪后补执行，元素卸载/超时则作废并 settle，绝不外溢），并把 ref 回调改为按 tab 缓存的稳定函数（避免无关重渲染反复解绑/重绑并触发同步调用）
- 修复：右面板内任一面板（浏览器/终端/文件树）出错不再把整个应用打成白屏——为面板内容加局部错误边界（紧凑兜底卡片 + 「重试」，切 tab 自动复位），顶层边界仅作最后兜底

- 修复：消息流新行与原行短暂重叠、点击会话偶发部分消息重叠——虚拟项位置此前靠 React 重渲染（落在本次绘制之后）回写测量结果，导致撑高内容的后续项有一帧仍按估算高度排版；现改为虚拟列表直接写 DOM 定位，并让首次定位等待测量收敛后才揭示内容
- 修复：设置页横向滑动条不可见且无法拖动——引用了从未定义的 `--slider-track-w` 导致轨道宽度塌陷为 0（Radix 按 Root 实测宽度换算指针位置，宽度为 0 即不可拖）；已补齐变量并加 fallback
- 修复：弹窗内下拉框鼠标移动无聚焦色——Radix 的 `data-highlighted` 依赖 item 获焦，而 item 经 Portal 挂在 Dialog 焦点陷阱之外，`focus()` 被立即拉回导致高亮被撤销；改用与焦点无关的 `:hover` 提供悬停底色（键盘导航仍走 data-highlighted）
- 修复：顺带补齐另三处"被引用但从未定义"的 CSS 变量（`--border-hover` / `--surface` / `--text-primary`），此前会让 hover 边框、浅底与错误文字色整条声明失效
- 修复：计划模式下点「运行诊断」整页报错 `e.map is not a function`——后端 `checks` 返回对象而前端按数组渲染；前端改为 dict/list 双形态归一化，并修正 API 类型声明
- 修复：点击会话后从顶部滚到底部——虚拟列表首帧从 offset 0 渲染再补滚；现按末项估算位置给出初始偏移，并在贴底完成前隐藏内容，直接呈现底部
- 修复：工具调用展开区内部滚动条看起来是隐藏的——`scrollbar-color` 可继承，被祖先 `.message-flow` 的 `transparent` 覆盖；现显式覆盖并收窄到 4px，默认可见
- 修复：消息定位刻度条 hover 时横条整体右移、且与 scrollspy 聚焦黑条叠加——改为一律左对齐、只让右端呈阶梯加宽，hover 期间只高亮鼠标所在那一条
- 优化：计划确认入口收进计划卡内部下方居中（「查看完整计划」「确认执行」同形状、去掉箭头），移除输入框上方的"计划已就绪"横幅；发新消息后确认按钮随状态自动消失
- 优化：任务进度与文件变更拆成两个独立浮层，各自聚焦展开；浮层标题块固定、内容区独立滚动，排版与间距重排
- 优化：上下文浮层瘦身——移除实时缓存率、缓存命中、输出、思考输出、模型、数据来源与占用构成明细，仅保留容量/百分比/进度条与平均缓存命中率
- 优化：悬浮胶囊略放大（高度约 34px），并为消息流恒定预留底部安全区，避免遮挡最新一行消息文字

## v0.5.7

- 修复：设置页横向拖动条不可用——滑块行缺确定宽度导致轨道塌陷成一个圆点（补齐宽度与命中区，thumb 加 hover/active/focus 反馈）
- 修复：消息流执行中 AI 回复不再显示复制/点赞按钮（须等本轮真正结束才出现，消除按钮随流式内容跳动）；用户消息操作行不受影响
- 修复：系统报错只保留两处——右上角浮窗（系统级）与消息流内错误卡（任务级）；移除标题栏与消息流之间的横幅（此前同一错误被渲染两遍）
- 修复：计划模式手动终止不再报"计划文档未生成"——服务端在判定方案文档前先识别用户取消/中断，按 interrupted 收尾而非 failed
- 修复：图片预览点击蒙层边缘可关闭（此前舞台层吞掉点击）、打开即聚焦关闭按钮、补键盘聚焦可见性、关闭后焦点归还触发元素、拖拽位移阈值防误关
- 修复：拖拽附件进输入框的边框错位——改为输入卡自身激活态（几何与圆角随卡片），不再外挂一层比输入卡大一圈的虚线遮罩
- 新增：搜索命中高亮（`--color-find-highlight` 系列此前从未被使用），当前命中项配色更强并自动居中
- 新增：页面/标签切换统一过渡（只动 transform/opacity，随三档动画设置降级）——侧栏导航页、会话切换、设置 tab、右面板 tab
- 优化：消息流进行中状态统一为单一状态行（呼吸点 + 文案），移除流式尾部三点光标；进度/压缩卡纳入插件 slot（可替换）
- 优化：排队消息展示重做——序号、模式标签、附件数、独立"立即发送/移除"图标按钮与入场动效
- 优化：上下文浮层重做——hover 即显（点击可钉住），基础态只给容量/百分比/进度条/平均缓存命中率，拿到供应商实测数据时展开实时缓存率与占用明细；压缩中圆环自身旋转（去掉"压缩中"文字），圆环由 26px 缩至 20px
- 优化：输入框上方任务进度/文件变更改为悬浮胶囊——智能分块（只有一边就只显一边）、两者皆无时不渲染、hover 弹浮层；进度数据改为仅由 AI 清单/步骤驱动（不再被用户消息文本覆盖）
- 优化：剩余原生下拉框迁移到 Radix Select（插件页、技能页）

## v0.5.6

- 设计语言 v7：新增圆角/控件高度/间距阶/页面宽令牌，圆角收敛为 4 档（输入 8、卡片与
  气泡 12、弹窗 14），全站间距统一走 7 档节奏
- 组件库：`components/ui` 从薄封装扩展为完整库——Input / Textarea / Field / Slider /
  Select / Checkbox / RadioGroup / Switch / Card / PageShell / IconButton / Collapse /
  Dialog / Menu / Tooltip / Markdown；输入类控件基于 Radix 无样式原语，补齐键盘可达性
  （滑块方向键、复选 Space、下拉/弹窗焦点陷阱）
- 设置页展示引擎：页面骨架改为 `PageShell` 数据驱动（取代 17 处内联重复），滚动容器
  唯一化并恒定预留滚动条宽度；统一页面宽度（去掉 680/960 双宽跳变）；修复副标题负
  margin 错位与卡片负 margin 分隔线外溢（布局割裂/异常换行根因），表格类内容加横向滚动兜底
- 弹窗族：Modal / ConfirmDialog / FormDialog 内部迁至 `ui/Dialog`（对外 API 不变），
  获得真实退场动画、焦点陷阱与关闭后焦点还原
- 消息流修复：操作行门控由「全局运行态」改为「本 turn 行状态」——任务异常终止、手动
  停止、回滚后最近一条用户消息均可见复制/回滚；AI 操作行归入 AI 回复块内部，不再与
  注入的用户消息合并为一个整体；最近一条用户消息操作行常显
- 计划模式修复：确认执行复用规划 turn（后端不换 turn），该轮行状态会停留在
  awaiting_confirmation，前端据此把「执行中」误判为「已结束」，使首条用户消息（即
  最近一条用户消息、操作行常显）在执行期间常驻复制/回滚按钮——门控改为「行状态非终态
  即视为运行中」，确认执行时就地进入运行态并广播 turn.updated(running) 同步行状态
- 空态首页与消息排版：输入卡去掉被切边的上边框、圆角与间距统一走令牌、问候语与 chips
  节奏收敛；markdown 代码块复制改为图标按钮
- 图标：新增 `IconAlertCircle` / `IconRingProgress`，收编消息流错误图标与上下文占用环

## v0.4.1

- 定时任务：cron 表达式完整解析（`*/n`、`a-b`、列表、月/周英文名），新增调度循环
  （原子领取防重复触发、错过策略、会话失效自动禁用），面板支持实时校验、下次运行
  倒计时、立即试跑与排程预览
- 技能：新增 `skill_view` 工具，AI 可按需加载技能正文并主动使用；注入 prompt 增加
  行为引导段与触发条件；技能面板支持编辑触发条件与 AI 可见性预览
- MCP：修复工具列表拉取签名错误；注入逻辑三份重复收敛为一处；只读/计划模式按风险
  等级注入只读类工具；面板新增健康状态、工具清单与手动刷新
- 权限模式：从 4 个硬编码模式升级为可配置模式（内置 4 模式可覆盖、支持自定义模式
  与工具白名单勾选），执行引擎与审批闸门同步外置，模式菜单动态化
- 项目索引：新增符号索引服务（Python AST + 多语言正则），新增 `symbol_search` /
  `outline` 工具，codebase_search 改为符号边界切块；诊断面板可查看/重建索引
- 记忆三层化：memory 支持 session / project / global 作用域、候选区（低置信可检索
  不注入）、作用域提升/降级；新增 `memory_write` 工具让 AI 主动记录记忆；
  `memory_search` 升级为三源联合检索；压缩块查看支持分页与关键词过滤
- 软件更新：更新前可见 Release Notes（内联 Markdown），新增更新历史时间线与下载
  进度条；升级后首启展示「本次更新」弹窗
- 界面：统一焦环规范、状态感知图标；动画时长阶梯化并收敛硬编码

## v0.4.0

- 项目任务驱动的多 Agent 协同编码工作台基础版本
