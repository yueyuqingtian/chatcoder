# AGENTS.md —— chatcoder 项目规则

本文件面向所有在本仓库工作的 AI 代理与开发者，定义不可违背的约定。优先级：本文件 > 个人习惯；与代码现状冲突时，先看是否有 `plan-*` 注释说明理由。

## 1. 项目结构与技术栈

- `client/`：React 18 + TypeScript + Vite + zustand 前端；样式集中在 `client/src/styles/`（设计令牌 `tokens.css` → `global.css` → `components.css` → `motion.css` → `focus.css` 按序加载）。
- `server/`：Python FastAPI 后端；`gateway/`（路由与 schema）、`services/`（业务）、`persistence/`（模型与迁移）、`orchestration/`（代理编排）。
- `electron/`：桌面壳；主进程 `main.cjs`、预加载 `preload.cjs`。
- `ai/`：规划文档（`chatcoder-plan-<session>-<turn>.md`），多步骤任务先写计划再实施。
- 前端通用 UI 组件统一在 `client/src/components/ui/`（Button / Input / Switch / Slider / Select / Dialog / Tooltip / Card …）。

## 2. 设计语言（前端改动必须遵守）

- 圆角只用令牌：控件 **8px**（`--r-md`）、卡片 **12px**（`--r-lg`）、浮层/弹窗 **14px**（`--r-pop`）、胶囊 `999px`；**禁止**硬编码 5/6/7/9/10px 等非令牌值。
- 按钮只用 `ui/Button` 或 `.btn` 系列：variant 负责配色、size 负责尺寸（高度与内边距走 `--btn-*` 令牌）；**禁止**新增页面私有按钮类（历史遗留的 `.perm-tab`、`.ts-mini-btn` 等逐步迁移）。
- 输入框 / 开关 / 滑块统一走 `ui/Input`、`ui/Switch`、`ui/Slider`，视觉与 `.ui-input` 对齐（同圆角、同高度、同悬停/聚焦行为）。
- 悬停提示统一用 `ui/Tooltip`；**禁止新增原生 `title`**（存量按迭代逐步替换）。
- **禁止用左侧装饰性细竖线**（`inset` 彩色边框、1~2px 色条、`border-left` 高亮）表达状态、分组或选中——这类“AI 味”装饰已被用户明确讨厌。层次一律用留白、间距、字号字重、图标与底色表达；状态用角标、图标色或文案表达。
- 动效统一引用 `--dur-*`（已含 `--motion-scale` 系数）；禁止 0 动画切换，禁止布局抖动——运行态指示、加载态、图标替换必须**预留固定槽位**。
- 操作一致性：设置页所有配置项「**修改即保存**」（输入类 300ms 防抖）；只有破坏性操作、批量操作、需要校验的复合表单（如供应商/模型/Key 表单弹窗）才保留显式按钮。
- 浮层（菜单/气泡/下拉）必须支持「点击外部关闭 + Esc 关闭」。

## 3. 版本发布与更新内容规范（重要）

发布新版本时必须同时更新 `CHANGELOG.md`，内容会呈现在应用内「关于 → 更新历史」与升级后首启的「本次更新」弹窗。更新内容必须满足：

1. **分组固定为三类，顺序不变**：
   - `### 新增`（能力/功能首次出现）
   - `### 优化`（体验/性能/文案改进）
   - `### 修复`（Bug 修复）
   无内容的组可省略；每组条目以 `- ` 开头。
2. **面向用户书写**：说明「用户能看到什么变化」，不写内部实现细节——类名、文件名、函数名、`plan-*` 编号一律不出现在更新内容里。
3. **条目格式**：一句话说清「做了什么 + 解决了什么问题」，不超过 60 字；同一主题合并为一条，避免逐文件罗列。
4. **破坏性变更**必须单独分组 `### 重要变更`（置于 `### 新增` 之前），并在条目末尾给出动作提示：「（需重新登录 / 需重启应用 / 需重新索引 / 需重新配置）」。
5. 版本号遵循语义化 `MAJOR.MINOR.PATCH`；影响用户既有配置或数据的改动提升 MINOR。
6. **版本号只认 `package.json`**：更新内容一律归入 `package.json` 当前版本（尚未正式发布的版本）区块，同一批未发布改动持续追加在该区块内；**不得**擅自新增或提升版本号——例如凭空写出 `## v0.8.0`、`## v0.9.0` 另起区块，或把一批未发布改动拆成多个版本块。版本号的提升与定版，只在正式发布时由维护者统一调整。
7. 发布前检查：更新内容与 `ai/` 下当期计划文档的验收项**一一对应**，不遗漏、不夸大、不写未交付的项。

## 4. 代码与注释约定

- 注释与 UI 文案一律简体中文；注释引用 `plan-<session>-<turn>` 编号说明「为什么这样改」，不描述「改了什么」。
- 不引入未使用的依赖；必须引入第三方库时，在设计方案中说明理由与体积影响，并统一改造为项目设计语言。
- 提交前必须通过：`client` 的 `npm run typecheck`（或 `npx tsc --noEmit`）与 `npm run build`；`electron` 改动执行 `node --check electron/main.cjs`。
- 不修改与本任务无关的模块；发现顺带问题时记录到计划文档，另行处理。

## 5. 流程约定

- 多步骤任务先进行任务规划（目标 / 现状调查 / 分阶段计划 / 涉及文件 / 验收标准），确认后按阶段实施。
- 每阶段完成即做类型检查与关键路径走查；回归优先覆盖：左右面板拖拽、消息流虚拟化、设置页滚动与表单。
- 长驻进程（dev server / watch / 后端服务）一律后台启动并保留日志；失败时先读完整报错定位根因，禁止盲目重试。
- 进行前端设计时，要从用户视角分析，分析怎么改更优雅，简洁，并且从用户操作角度分析，有没有问题，操作是否合理，分析用户的操作路线，第一眼看到页面知不知道怎么操作，用户会怎么操作。
